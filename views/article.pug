extends layout 

block content
    h1= article.title
    i 
        p Written by #{author}
    
    hr
    p= article.body
    hr
    if user
        if user.id==article.author  
     

        
            a.btn.btn-outline-secondary(href='/articles/edit/'+article._id)
                i Edit  
            a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete